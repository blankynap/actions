name: Share variables between jobs

on:
  workflow_dispatch:
    inputs:
      stage:
        description: 'Stage'
        required: true
        default: 'prod' 
        type: choice
        options:
        - dev
        - prod

jobs:
  job1:
    runs-on: ubuntu-latest
    outputs:
      deployment_tag: ${{ steps.deployment_tag_id.outputs.deployment_tag }}
    steps:
      - id: deployment_tag_id
        run: echo "deployment_tag=$(date +%Y%m%d-%H%M)" >> "$GITHUB_OUTPUT"

  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - env:
          OUTPUT1: ${{needs.job1.outputs.deployment_tag}}
        run: |
          echo "$OUTPUT1"
          echo ${{ inputs.stage }}
